<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.vibepay.repository.MemberRepository">

    <resultMap id="memberResultMap" type="com.vibepay.domain.Member">
        <id property="id" column="id"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="name" column="name"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findByEmail" resultMap="memberResultMap">
        SELECT id,
               email,
               password,
               name,
               created_at,
               updated_at
        FROM member
        WHERE email = #{email}
    </select>

    <select id="findById" resultMap="memberResultMap">
        SELECT id,
               email,
               password,
               name,
               created_at,
               updated_at
        FROM member
        WHERE id = #{id}
    </select>

    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO member (email, password, name, created_at, updated_at)
        VALUES (#{email}, #{password}, #{name}, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)
    </insert>

</mapper>
